{
  "name": "scrapshot-prototype",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5174",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5174",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "matter-js": "0.20.0"
  },
  "devDependencies": {
    "@types/matter-js": "0.20.2",
    "jsdom": "26.1.0",
    "typescript": "5.8.3",
    "vite": "6.4.1"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
